/**
 * Escribe una arrow function que reciba un número y devuelva su doble.
 */

const sumaDoble = (num) => {
  let doble = num * 2; // Aqui se multiplica el numero por dos
  return doble;
};

console.log(sumaDoble(5));
