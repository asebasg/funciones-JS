/**
 * Escribe una declaración de función que reciba un nombre como parámetro
 * y devuelva un saludo.
 */

function saludo(nombre) {
  return `Hola, ${nombre}, ¿cómo te va?`
}

console.log(saludo("Sebastian"));
